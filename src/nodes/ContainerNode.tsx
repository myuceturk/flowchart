import { createNodeComponent } from './createNodeComponent';

export default createNodeComponent({
  type: 'container',
  className: 'node-frame node-frame--container',
  labelPlaceholder: 'Container',
});
