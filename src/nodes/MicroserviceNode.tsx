import { createNodeComponent } from './createNodeComponent';

export default createNodeComponent({
  type: 'microservice',
  className: 'node-card node-card--microservice',
  labelPlaceholder: 'Microservice',
});
